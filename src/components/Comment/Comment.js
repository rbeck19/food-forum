export default function Comment({ comment }) {
    console.log(comment)
    // console.log(comment.recipe)


    // if (comment.recipe === recipe.id)

    return (
        <h2>{comment.note}</h2>
    )
}